version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci  # or "npm install" if you're not using a lockfile
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
